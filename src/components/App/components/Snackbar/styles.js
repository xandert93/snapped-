import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  snackbar: {
    // [theme.breakpoints.down('sm')]: {
    // top: '50%',
    // left: '50%',
    //translate worked on desktop (and in responsive view), but not actual phone. Weird.
    //   transform: 'translate(-50%, -50%)',
    // },
  },
}));
