import { AAA, BBB, CCC, DDD, EEE } from './types';

let initialState = {};

export default (state = initialState, { type, payload }) => {
  switch (type) {
    case 0:
      return { ...state };
    case 0:
      return { ...state };
    case 0:
      return { ...state };
    case 0:
      return { ...state };
    case 0:
      return { ...state };
    default:
      return state;
  }
};
