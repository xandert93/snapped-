import { AAA, BBB, CCC, DDD, EEE } from './types';

export const x = () => (dispatch) => dispatch({ type: '' });

export const x = (bool) => (dispatch) => dispatch({ type: '', payload: bool });

export const x = (message) => (dispatch) =>
  dispatch({ type: '', payload: message });

export const x = (message) => (dispatch) =>
  dispatch({ type: '', payload: message });

export const x = () => (dispatch) => dispatch({ type: '' });
