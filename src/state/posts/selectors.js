export const selectIsPostsLoading = (state) => state.posts.isPostsLoading;

export const selectTimelinePosts = (state) => state.posts.timeline;

export const selectUserPosts = (state) => state.posts.user;
