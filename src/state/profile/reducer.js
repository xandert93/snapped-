import {} from './types';

let initialState = {};

export default (state = initialState, action) => {
  switch (action.type) {
    case 0:
      return;
    case 1:
      return;
    case 2:
      return;
    default:
      return state;
  }
};
